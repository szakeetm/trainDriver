pages:
  stage: deploy
  image: alpine:latest
  script:
    - mkdir public
    - cp index.html game.js styles.css public/
    - if [ -d screens ]; then cp -r screens public/; fi
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'